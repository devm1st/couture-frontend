// import clsx from "clsx";

// export const Card = ({ className, children, ...props }) => (
//   <div
//     className={clsx(
//       "rounded-2xl border border-white/10 bg-white/[0.04] shadow-lg shadow-black/30",
//       className
//     )}
//     {...props}
//   >
//     {children}
//   </div>
// );

// export const CardHeader = ({ className, children }) => (
//   <div className={clsx("p-5 border-b border-white/10", className)}>
//     {children}
//   </div>
// );

// export const CardTitle = ({ className, children }) => (
//   <h3 className={clsx("text-lg font-semibold text-white", className)}>
//     {children}
//   </h3>
// );

// export const CardDescription = ({ className, children }) => (
//   <p className={clsx("text-sm text-white/60 mt-1", className)}>{children}</p>
// );

// export const CardContent = ({ className, children }) => (
//   <div className={clsx("p-5", className)}>{children}</div>
// );

// export const CardFooter = ({ className, children }) => (
//   <div className={clsx("p-5 border-t border-white/10", className)}>
//     {children}
//   </div>
// );
